# PROJECT 1990 — 설계서 (living spec)

> 이 문서는 **현재 `src/engine.js`의 실제 동작**을 기술한다. 엔진이 바뀌면 여기도 함께 고친다.
> 수치는 모두 게임 밸런스용 창작값이며, 구단명만 실제 명칭이다.

---

## 0. 설계 원칙

`src/engine.js` 헤더에 박혀 있는 7원칙이 이 게임의 헌법이다.

1. 엔진은 결과를 **결정**한다. 텍스트는 결과를 **설명**한다.
2. 능력은 **평균 능력(Current Ability) 단일 수치**. 세부 스탯은 두지 않는다.
3. 잠재력(PA)·유리몸·빅매치 멘탈은 히든. 은퇴 후 공개.
4. 재능이 인생을 결정하지 않는다: 환경 × 선택 × 기회 × 운.
5. 가정환경은 능력치가 아니라 **"기회에 대한 접근성"** 으로 작동한다.
6. 16세 이후의 중심 선택은 매 시즌 이적시장이다.
7. DOM 의존 0 — 브라우저 UI와 헤드리스 배치 러너가 같은 엔진을 공유한다.

**판정과 서술의 분리** — 모든 확률·수치 판정은 `src/engine.js`에서만 일어난다.
`web/app.js`는 상태를 표시만 하고 어떤 판정도 하지 않는다. 그래서 `tools/batch.mjs`가
UI 없이 같은 엔진으로 수천 회 검증할 수 있다.

---

## 1. 타임라인 (Hybrid Turn System)

```
0~15세   → 헤드리스 시뮬 → 배경설정 산문 (플레이 없음)
16~29세  → 반년 단위 (여름/겨울 이적시장 중심)
30~36세  → 1년 단위 (에이징 커브, 커리어 마무리)
```
플레이 구간은 16세부터, 1회차 약 **35턴**. `cadenceOf(age)`가 반년/1년을 가른다.

---

## 2. 능력 모델

### 시작 능력 고정 + 잠재력 = 성장 속도
```js
START_OVR = 50               // 전원 동일하게 출발
potential = rng.int(58, 99)  // 균등분포, 히든 (플레이 중 비공개)
growthSpeed(p) = clamp(0.75 + (PA − 58) × 0.01622, 0.6, 1.45)
```
모두 50에서 시작한다. 잠재력은 "도달할 상한"이 아니라 능력이 **얼마나 빨리 오르는가**를
정한다. 균등분포라 특정 구간에 몰리지 않는다.

### 성장 공식
```js
delta = clamp(years × curve × (gap/100) × mult × noise
              × growthSpeed(p) × 6.4 × stressPen × injPen × semiPen, 0, 14)
```
피크 28세(`PEAK_AGE`). 이후 `years × (0.35 + (age−28)×0.30) × (1 + (60−pro)/200)` 로 하강 —
좋은 리그에서 뛴 선수일수록 완만하게 떨어진다.

### 환경 천장 (Environmental Ceiling)
```js
devEnv  = 11~22세에 겪은 훈련 환경의 지수이동평균
ceiling = PA × (0.72 + 0.28 × normalize(devEnv))   // 하한 = START_OVR + 3
```
잠재력이 높아도 열악한 환경에서만 자랐다면 그 수치에 닿지 못한다. **원칙 4를 수치로
옮긴 한 줄.** `devEnv`는 11~22세에만 누적된다 (의도된 창).

### 세미프로 락 (18세 진입 실패 시)
```js
proEntryScore(g)     // 13개 요인 종합, 문턱 >= 26
semiProUntil = year + 6      // 3부 6년 고정
semiPen = 0.5                // 그 기간 성장 속도 절반
// div ≤ 2 진출은 24세부터 허용
```
18세 프로 진입 게이트를 넘지 못하면 3부에 6년 묶인다. 취업·대학진학·가업 계승 같은
이탈 선택지는 없다 — 축구를 계속한다.

---

## 3. 16세 배경설정 산문화

### 핵심 원칙: 배경은 "쓰는" 게 아니라 "돌린 결과를 쓰는" 것
랜덤 텍스트 조합은 작위적으로 읽힌다. 대신 0~15세를 **헤드리스로 실제 시뮬레이션**하고
(선택은 성격 기반 AI), 그 결과 상태(능력·devEnv·부채·기억·소속 유소년팀)를 산문으로 렌더한다.
배경에 나온 사건이 **실제 상태값과 기억으로 남아** 나중에 회수된다 (예: 배경의 라 마시아
탈락 → 수년 뒤 그 팀을 상대로 만나는 이벤트).

### 렌더 예시 (실제 출력 형태)
```
━━━ 16세까지의 이야기 ━━━

라민 팔(Lamine Fall). 1990년 7월 18일, 바르셀로나 산츠 지구.
...
2002년 여름, 열두 살에 FC 바르셀로나 유소년 테스트를 봤다. 400명이 왔다.
접수처에서 서류를 두 번 확인받았다. 앞의 아이들은 한 번이었다.
사흘 뒤 리포트가 왔다.
  "나쁜 선수가 아니다. 다만 현재 신체조건과 포지션 경쟁을 고려하면,
   지금 우리 시스템에 넣어야 할 필연성이 낮다."
...
  현재 능력 50 · 소속 CE 사바델 유소년 · 가계 부채 없음
━━━━━━━━━━━━━━━━━━━━━━
```
가정환경 7종 × 유소년 경로 × 사건 조합으로 매번 달라진다.

---

## 4. 이벤트 서사 템플릿

모든 주요 이벤트는 이 구조를 따른다. 지문은 **보고체(`~다`)**, 선택지는 **독백체 결단
대사**(`~/.claude/skills/script/SKILL.md` 규칙). 각 선택지는 `[이번 기간에 미치는 영향]` 을
실제 엔진 효과에서 생성해 표시한다 — 설명과 판정이 어긋나지 않는다.

```
① 시대·리그 맥락    ② 상황 제시    ③ 가정환경 반영(빚/부모/형제)
④ 주변 인물 반응(에이전트·감독·가족)    ⑤ 주인공 심리
+ 각 선택지에 인과 명시 (무엇이 바뀌고 / 다음에 무엇이 올지)
```

---

## 5. 시스템

### 가정환경 (7종)
이민자/원주민 × 하위/중위/상위 + 혼혈·다문화층. 각 환경은 `money·safety·academic·
scoutBias` 등으로 기회 접근성을 바꾼다. 능력치 보너스는 주지 않는다.

### 이적시장 / 오퍼
- 여름·겨울 이적시장이 반년 턴의 중심. `generateOffers(g, 3)` — 기본 **3개**.
- 팀 평균 능력(OVR)과 선수 능력을 비교해 주전/경쟁/백업/오퍼없음을 판정. 유망주(21세 이하)
  할인 적용. 팀의 잠재력(Potential)과 현재 OVR을 분리해 유망주 영입 논리를 만든다.
- 리그 티어 계층(`LEAGUE_TIER`): ENG/ESP/GER/ITA 1부 = Tier1, FRA는 예외적으로 PSG·Monaco만
  Tier1급. 이적 시 티어 격차 제한(±5 차단, 단 출전 부족 시 예외).
- **영구 이적**에는 오퍼 금지 룰 적용, 자국 경제위기 시 스페인 구단 매각 의지↑.
- UI에 항상 노출: `Real Betis (1부) 팀 평균 71 · 당신 68 → 로테이션 후보`

### 부채 (debt)
```js
econ.debt, econ.debtRate     // 사채 0.34 / 은행 0.09 / 구단 가불 0.0
```
매 기간 이자 발생, 자산으로 일부 자동 상환. 완제 시 `[빚을 다 갚았다]` 이벤트.
발생원: 형제 보석금, 아버지 고용 변동, 의료비 등. 부채는 이적 협상력을 떨어뜨린다.

### 아버지 고용 (확률적)
2008 위기 등에서 아버지 고용은 **결정론적 실직이 아니라** 가중 추첨이다:
`KEEP / PAYCUT / HOURS / FIRED / MOVED`. 환경 안전도와 압박이 확률을 바꾼다. "위기 →
반드시 실직 → 반드시 프로行" 같은 정해진 레일을 제거하기 위한 설계.

### 형제 서브플롯
배경에서 형/동생이 생성되어 별도 트랙(안정→이탈→사건→결말)으로 진행. 주인공의 지원이
형 트랙에 영향을 주고, 30대에 [형의 부고/재회]로 회수된다. 발생 빈도 상한 있음.

### 부상 (유형화)
타박/근육/발목/골절/햄스트링/아킬레스/십자인대. 확률은 조건부:
`기본률 × 출전부하 × 유리몸지수 × 나이 × 스트레스`. 중상은 즉시 OVR −5~−10 + 잠재력
손실 + 유리몸 지수 상승을 남긴다 (heavy-tail 분포).

### 챔피언스리그 + 개인 수상
- 리그 순위 → 다음 시즌 UCL/유로파/미참가. UCL 라운드 시뮬 + 라운드별 개인 평점.
- **발롱도르**: 매년 전 세계 1명(글로벌 경쟁 롤). 자격 게이트 = UCL/국가대항전 우승 +
  주전(출전 70% 이상). T1 엔딩의 핵심 조건. 그 외 리그 MVP/득점왕/베스트11 등.

### 승격·강등
커리어 테이블 한 줄이 아니라 독립 이벤트. 최하위 리그에서는 "강등" 대신 "승격 실패/잔류".
승격 판정은 리그 parity(1부 .15 / 2부 .22 / 3부 .30) + 출전 비중 기반 선수 lift.

### 국가대표
- 소집은 **포지션 경쟁 우선**: 같은 포지션 대표팀 평균과 먼저 비교, 그다음 전체 평균·폼·
  평판·국제 경험 가중. 포지션 희소성(GK·CB 우대)·폼 폭발은 가점, 부상은 감점.
- 평균보다 능력이 낮아도 소집 확률이 0이 아니다 (예외 여지).
- 소집 후 역할: 대표팀 평균 대비 격차로 **CORE / LEADER / SQUAD / FRINGE** 4단계, 서술이
  각각 다르다. 고정 문구("나보다 유명한 이름이 스무 개…") 제거.
- **3부 소속은 A매치에 소집되지 않는다.** 캡은 시즌별 기록(누적 저장 버그 수정 완료).

### 역사 이벤트 (61개)
컷신이 아니라 세계의 확률분포를 바꾼다. 각 이벤트는 발생 시 실제 엔진 효과로부터
`[이번 기간에 미치는 영향]` 을 생성해 보여준다.

| 연도 | 사건 | 효과(예) |
|---|---|---|
| 2001 | FIFA 18세 미만 국제이적 제한 | 18세 미만 해외 이적 차단 |
| 2008 | 세계 금융위기 | 가계 압박↑ → 아버지 고용 변동 추첨 |
| 2020 | 팬데믹 | 시즌 경기 수 ×0.72 |
| 2023~ | 사우디 자본 유입 | 특정 조건 주급 대폭 상승 |

---

## 6. 엔딩 (36 매트릭스)

**4개 가정환경 그룹 × 9개 티어 = 36 셀.** 실패는 게임오버가 아니다.

```
T1 All-Time GOAT   T2 Global Megastar   T3 대륙급 에이스
T4 1부 스타터       T5 하부리그 저니맨    T6 로컬 벤치 워머
Bad End 뒷골목      Normal End 생활인     Success End 타 분야 성공
```
은퇴 시 커리어 전기 자동 생성(연도별 기록·경로·전환점·가장 후회되는 순간·가장 중요했던
사람·타임라인) + 마지막에 PA와 "쓰지 못한 재능" 공개.

---

## 7. 밸런스 목표 vs 실측

`node tools/batch.mjs` 기준 (1,200회).

| 지표 | 목표 | 실측 |
|---|---:|---:|
| 발롱도르 (T1) | ~5% | 5.3% |
| 월드클래스 (T2) | ~10% | 7.6% |
| 1부 정착 | 40~50% | 47.1% |
| 프로 진출(최종) | — | ~85% |

> **알려진 트레이드오프:** T1/T2/1부 정착을 동시에 목표치에 맞추기 어렵다. 세미프로 6년
> 고정(성장 ×0.5)이 빅클럽 도달을 억제해 T2가 목표보다 낮은 편. 환경별 프로 진출률은
> 83~90%로 평탄하다 — 스페인 단일 트랙 + 시작능력 50 고정의 결과. 환경은 프로 진입
> 여부보다 성장 천장·Bad End 위험·기회 접근성을 가른다.

---

## 8. 미구현 / 다음 순서

- **세계 리그 현황 대시보드** — 연도별 각 리그 최강 클럽 + UCL 우승팀 표시 (미구현).
- **영입 판정 정교화** — 구단 재정·유소년 정책·포지션별 팀 내 경쟁자 반영 (부분 구현: 현재는
  팀 평균·잠재력·나이·출전시간까지).
- LLM 서술 레이어 / 선택지 동적 생성.
- 이탈리아·한국(군 문제) 트랙.
- 세이브/불러오기, 명예의 전당.

---

## 9. 파일 지도

| 파일 | 역할 | 편집 |
|---|---|---|
| `src/engine.js` | 시뮬레이션 엔진 — 유일한 진실의 원천 | 직접 |
| `web/app.js` · `web/style.css` | UI 레이어 / 시각 디자인 | 직접 |
| `tools/batch.mjs` | 헤드리스 검증기 | 직접 |
| `build.mjs` | 인라인 빌드 → `docs/`, `dist/` | 가끔 |
| `docs/index.html` · `dist/*.html` | **생성물** — 손대지 않는다 | 빌드로 갱신 |
| `README.md` · `DESIGN.md` | 사람이 읽는 문서 — 엔진과 함께 갱신 | 직접 |
